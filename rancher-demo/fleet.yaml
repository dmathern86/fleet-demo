defaultNamespace: dmathern

helm:
  chart: rancher-demo
  repo: https://dmathern86.github.io/fleet-demo
  releaseName: rancher-demo
  version: 2.7.0
  values:
    app:
      title: Fleet App
      cluster: global.fleet.clusterLabels.management.cattle.io/cluster-display-name
    ingress:
      enabled: true
      host: nip.io
targetCustomizations:
- name: pre-prod
  helm:
    values:
      app:
        pets: cowmeleons
  clusterSelector:
    matchLabels:
      env: qa